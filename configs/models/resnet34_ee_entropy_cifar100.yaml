name: resnet34_ee_entropy_cifar100
type: early_exit
architecture: resnet34

weights:
  path: ./checkpoints/resnet34_ee_entropy_cifar100.pth

early_exit:
  criterion: entropy
  confidence_threshold: 0.9
